# 金石篆刻社官网 - 开发指南

## 项目概述

本项目是金石篆刻社的官方网站，基于 React 18 + TypeScript + Vite + Tailwind CSS 构建。网站采用组件化开发模式，支持路由导航和 Markdown 文章自动加载。

## 目录结构

```
├── public/              # 静态资源目录（构建后直接复制到dist）
│   ├── admin/           # Decap CMS 管理后台（/admin/ 登录后在线管理文章）
│   │   ├── index.html   # 后台入口页（管理员登录门禁 + 加载 Decap CMS）
│   │   └── config.yml   # Decap CMS 配置（DecapBridge git-gateway 认证、文章字段、图片上传目录）
│   ├── articles/        # Markdown 文章文件（仅用于下载标准格式模板）
│   │   └── 标准格式.md  # 文章标准格式模板（供下载使用）
│   ├── images/
│   │   ├── articles/    # 文章配图（随文章上传）
│   │   └── static/      # 静态图片（网站不变元素，如社长/老师照片）
│   ├── logo.png         # 网站 LOGO
│   ├── 题字.svg         # 网站标题题字（顶部导航栏）
│   ├── favicon.svg      # 浏览器标签页图标
│   └── 篆刻社社务手册.pdf # 社务手册 PDF（规章制度页面嵌入展示）
├── src/                 # 源代码目录
│   ├── articles/        # Markdown 文章文件（上传后自动扫描加载）
│   │   ├── *.md         # 文章文件（需包含 YAML Frontmatter）
│   │   └── 标准格式.md  # 文章标准格式模板（扫描时自动排除）
│   ├── components/      # 公共组件（首页和各页面共用）
│   │   ├── Banner.tsx           # 轮播图组件（最近6篇金石资讯，胶囊进度条）
│   │   ├── NewsSection.tsx      # 金石资讯组件（首条显示缩略图）
│   │   ├── MemberNews.tsx       # 社员动态组件
│   │   ├── AcademicResources.tsx # 学术资源组件
│   │   ├── ActivityBanners.tsx  # 活动横幅组件（点击跳转搜索对应关键词）
│   │   ├── BottomModules.tsx    # 底部模块组件（视频中心、名家讲堂、社藏菁华、在线展厅，待办占位）
│   │   ├── QuickLinks.tsx       # 快捷链接组件（合作申请、社员名录、社务征稿、社刊投稿）
│   │   ├── Header.tsx           # 顶部搜索栏组件（搜索框）
│   │   ├── Navigation.tsx       # 导航栏组件（LOGO/题字 + 主导航下拉菜单，居中定位 mt-14px 过渡 600ms）
│   │   ├── Footer.tsx           # 页脚组件
│   │   ├── HonorBadges.tsx      # 荣誉徽章组件
│   │   └── Pagination.tsx       # 翻页组件（用于所有二级目录页面）
│   ├── pages/           # 页面组件（每个文件对应一个路由页面）
│   │   ├── admin/       # 管理类页面（社团情况、规章制度、组织架构）
│   │   │   ├── SocietyPage.tsx          # 社团概况
│   │   │   ├── SocietyHistoryPage.tsx   # 师林撷英（指导老师介绍，并排显示）
│   │   │   ├── SocietyPresidentsPage.tsx # 历任社长
│   │   │   ├── PresidentDetailPage.tsx   # 社长个人介绍
│   │   │   ├── SocietyMembersPage.tsx    # 社员名录
│   │   │   ├── SocietyTimelinePage.tsx   # 大事记
│   │   │   ├── TimelineYearPage.tsx      # 大事记年份详情
│   │   │   ├── RulesPage.tsx            # 规章制度列表（纯文字链接）
│   │   │   ├── RulesCharterPage.tsx     # 篆刻社章程详情（五章十四条）
│   │   │   ├── RulesManualPage.tsx      # 社务手册详情（iframe 嵌入 PDF）
│   │   │   ├── OrganizationPage.tsx      # 社务委员会（时任社长副社长名单）
│   │   │   └── OrganizationStandingPage.tsx # 社团智库（历任社长副社长两列表格）
│   │   ├── NewsPage.tsx                 # 金石资讯（含首图缩略图，每页5篇）
│   │   ├── NewsMemberPage.tsx           # 社员动态（纯文字列表，每页10篇）
│   │   ├── NewsActivityPage.tsx         # 活动动态（含首图缩略图，每页5篇）
│   │   ├── NewsGovernmentPage.tsx       # 政务信息（纯文字列表，每页10篇）
│   │   ├── NewsNoticePage.tsx           # 公告公示（纯文字列表，每页10篇）
│   │   ├── AcademicPage.tsx             # 研讨文集（含首图缩略图，每页5篇）
│   │   ├── AcademicSubmissionPage.tsx   # 社务征稿
│   │   ├── JournalPage.tsx              # 在线社刊（封面网格，每页5篇）
│   │   ├── JournalSubmissionPage.tsx    # 社刊投稿
│   │   ├── CooperationPage.tsx          # 合作申请
│   │   ├── SearchPage.tsx               # 搜索结果
│   │   └── ArticlePage.tsx              # 文章详情（*** 重定向、链接自动换行、正文从构建加载）
│   ├── lib/             # 工具函数和数据
│   │   ├── articles.ts       # 文章解析工具函数（含日期格式自动修正）
│   │   ├── articleData.ts    # 文章数据自动加载（含 HMR 热更新支持）
│   │   └── utils.ts          # 通用工具函数
│   ├── App.tsx          # 应用入口和路由配置（含 HomePage 内联组件）
│   ├── main.tsx         # React 入口文件
│   └── index.css        # 全局样式和 Tailwind 指令
├── tailwind.config.js   # Tailwind CSS 配置（自定义颜色等）
├── vite.config.ts       # Vite 配置（构建、插件等）
├── tsconfig.json        # TypeScript 配置
├── package.json         # 项目依赖和脚本
├── 启动本地网页.bat     # 一键启动开发服务器
├── 启动生产版本.bat     # 一键启动生产版本
└── 网站开发指南.md      # 本文件
```

## 快速开始

### 安装依赖

```bash
npm install
```

### 启动开发服务器

```bash
npm run dev
# 或双击 启动本地网页.bat
```

访问 http://localhost:5173/ 查看网站。

### 构建生产版本

```bash
npm run build
# 或双击 启动生产版本.bat
```

构建产物位于 `dist/` 目录。

### 部署到 GitHub Pages

本仓库名为 `kingstone-society.github.io`，属于 GitHub Pages 组织/用户主页站点，网站部署在根路径 `https://kingstone-society.github.io/`，因此 Vite 的 `base` 保持默认 `/` 即可，无需修改。

**部署方式（推荐，自动部署）：**

1. 将代码推送到 GitHub 仓库（main 分支）
2. 在仓库 `Settings → Pages → Build and deployment → Source` 中选择 **GitHub Actions**
3. 之后每次推送 main 分支，GitHub Actions 会自动执行 `.github/workflows/deploy.yml`，构建并发布网站

**手动部署：**

本地执行 `npm run build` 后，将 `dist/` 目录内容上传即可（需先在 Settings → Pages 中设置 Source 为对应分支/目录）。

**SPA 路由说明：**

网站使用 React Router 的 history 模式（BrowserRouter）。GitHub Pages 是纯静态托管，直接访问或刷新子页面（如 `/news`、`/article/xxx`）会返回 404。为此，构建时会自动将 `index.html` 复制为 `404.html`（GitHub Pages 的自定义 404 页面机制），使任意路径都能加载应用并正确渲染对应页面（HTTP 状态码仍为 404，但页面内容正常，不影响使用）。

## 核心功能

### 1. 文章自动加载系统

#### 上传文章

将 `.md` 文件放入 `src/articles/` 目录即可。开发模式下保存任意文件触发 HMR 即可刷新文章列表；生产版本需执行 `npm run build` 重新构建。

**重要：文章必须放在 `src/articles/` 目录下**，同时配图放在 `public/images/articles/` 目录下。文章会在构建时通过 `import.meta.glob` 自动扫描加载。

#### 文章格式

文章需包含 YAML Frontmatter 头部：

```yaml
---
title: 文章标题
subtitle: 副标题（可选）
date: 2026-06-20
author: 作者名
slug: 文章别名（用于URL，可选）
header-img: /images/articles/封面图.png
tags: [标签1, 标签2]
section: [一级板块, 二级板块]
---

文章正文...
```

> 注意：date 字段请使用纯日期格式 `YYYY-MM-DD`，不要包含时间戳。

#### 板块分类

| 一级板块 | 二级板块 | 对应页面 | 轮播图 | 文章末尾首图 |
|----------|----------|----------|--------|-------------|
| 金石资讯 | (无二级) | /news | ✅ | ✅ |
| 金石资讯 | 社员动态 | /news/member | ✅ | ✅ |
| 金石资讯 | 活动动态 | /news/activity | ✅ | ✅ |
| 金石资讯 | 政务信息 | /news/government | ✅ | ✅ |
| 金石资讯 | 公告公示 | /news/notice | ✅ | ✅ |
| 社务研讨 | 研讨文集 | /academic | ❌ | ❌ |
| 金石艺丛 | 在线社刊 | /journal | ❌ | ❌ |

#### 正文格式

- 默认首行缩进 2em
- `{内容}` - 居中显示
- `→内容` - 右对齐显示
- `**内容**` - 加粗
- `*内容*` - 斜体
- `***内容***` - 加粗斜体
- `@@字号内容@@` - 自定义字号
- `独立一行的图片路径` - 在文章中插入图片（见下方"文章插图"）

#### 文章插图

在正文中，独占一行写入图片路径即可插入图片（居中、自适应宽度）：

```markdown
正文段落...

/images/articles/example.jpg

继续正文...
```

支持的图片后缀：jpg、jpeg、png、gif、webp、svg、bmp。

### 2. 图片管理

#### 静态图片

网站不变的元素图片放入 `public/images/static/`：
- 社长照片：如 `qmf.jpg`、`lxy.jpg`
- 指导老师照片：如 `zjq.jpg`、`mjy.jpg`
- 社徽图片：如 `emblem1.jpg`、`emblem2.png`
- 其他固定元素图片

#### 文章配图

随文章上传的图片放入 `public/images/articles/`，在文章 frontmatter 的 `header-img` 字段或正文中引用时使用绝对路径 `/images/articles/xxx.jpg`。

### 3. 页面结构

#### 首页组件

首页由多个组件组合而成，每个组件负责一个区域：

- `Banner.tsx` - 顶部轮播图（取最近6篇金石资讯文章首图，胶囊进度条：激活态 h-2/w-8、非激活态 h-2/w-2、间距 space-x-3，底部标题 text-sm）
- `NewsSection.tsx` - 金石资讯区域（首条文章显示缩略图，底部 学校官网/星川庐 链接）
- `MemberNews.tsx` - 社员动态区域
- `AcademicResources.tsx` - 学术资源区域（研讨文集 + 金石艺丛）
- `ActivityBanners.tsx` - 活动横幅（点击自动搜索"春日雅集"/"社庆"/"秋季雅集"）
- `BottomModules.tsx` - 底部模块（视频中心、名家讲堂、社藏菁华、在线展厅，均为待办占位符）
- `QuickLinks.tsx` - 快捷链接（合作申请 → /cooperation、社员名录 → /society/members、社务征稿 → /academic/submission、社刊投稿 → /journal/submission）

#### 二级目录页面翻页规则

所有二级目录页面均使用 `Pagination.tsx` 翻页组件，翻页规则如下：

| 页面 | 每页篇数 | 展示形式 |
|------|---------|---------|
| 金石资讯（/news） | 5 | 含首图缩略图 |
| 社员动态（/news/member） | 10 | 纯文字列表 |
| 活动动态（/news/activity） | 5 | 含首图缩略图 |
| 政务信息（/news/government） | 10 | 纯文字列表 |
| 公告公示（/news/notice） | 10 | 纯文字列表 |
| 研讨文集（/academic） | 5 | 含首图缩略图 |
| 在线社刊（/journal） | 5 | 封面网格 |

#### 二级页面通用结构

所有二级页面都包含：
- 面包屑导航（显示当前位置）
- 左侧侧边栏菜单（红色状态卡样式表示当前选中）
- 右侧主内容区

### 4. 路由配置

路由在 `src/App.tsx` 中配置，使用 React Router 6：

```tsx
<Routes>
  <Route path="/" element={<HomePage />} />
  <Route path="/society" element={<SocietyPage />} />
  <Route path="/society/history" element={<SocietyHistoryPage />} />
  <Route path="/society/presidents" element={<SocietyPresidentsPage />} />
  <Route path="/society/presidents/:name" element={<PresidentDetailPage />} />
  <Route path="/society/members" element={<SocietyMembersPage />} />
  <Route path="/society/timeline" element={<SocietyTimelinePage />} />
  <Route path="/society/timeline/:year" element={<TimelineYearPage />} />
  <Route path="/news" element={<NewsPage />} />
  <Route path="/news/member" element={<NewsMemberPage />} />
  <Route path="/news/activity" element={<NewsActivityPage />} />
  <Route path="/news/government" element={<NewsGovernmentPage />} />
  <Route path="/news/notice" element={<NewsNoticePage />} />
  <Route path="/academic" element={<AcademicPage />} />
  <Route path="/academic/submission" element={<AcademicSubmissionPage />} />
  <Route path="/journal" element={<JournalPage />} />
  <Route path="/journal/submission" element={<JournalSubmissionPage />} />
  <Route path="/rules" element={<RulesPage />} />
  <Route path="/rules/charter" element={<RulesCharterPage />} />
  <Route path="/rules/manual" element={<RulesManualPage />} />
  <Route path="/organization" element={<OrganizationPage />} />
  <Route path="/organization/standing" element={<OrganizationStandingPage />} />
  <Route path="/cooperation" element={<CooperationPage />} />
  <Route path="/search" element={<SearchPage />} />
  <Route path="/article/:slug" element={<ArticlePage />} />
</Routes>
```

### 5. 搜索功能

- 顶部搜索框（`Header.tsx`）支持按标题、作者、标签、板块搜索
- 搜索结果页面：/search（`SearchPage.tsx`）
- 首页活动横幅按钮点击后自动跳转搜索对应关键词

### 6. 文章重定向

如果文章正文第一行是 `***`，则自动重定向到第二行指定的 URL：
- 第二行为外部链接（`http://`/`https://`）→ 使用 `window.location.replace` 跳转
- 第二行为内部路径（如 `/society`）→ 使用 React Router `navigate` 跳转
- 适用于需要将文章链接转发到外部网站的场景

### 7. 标准格式下载

在社务征稿页面（`AcademicSubmissionPage.tsx`）提供"下载标准格式模板"按钮，点击可下载 `public/articles/标准格式.md`。

### 8. 社务手册 PDF

`public/篆刻社社务手册.pdf` 通过 `<iframe>` 嵌入在规章制度 → 社务手册页面（`RulesManualPage.tsx`）中直接浏览。

### 9. 后台管理（Decap CMS）

网站集成了 Decap CMS（原 Netlify CMS）作为文章管理后台，管理员可以在线创建、编辑、发布文章，无需本地操作。

#### 入口与账号

- 入口：首页右上角"登录"按钮，或直接访问 `/admin/`
- 管理员账号：`0101`
- 密码：`qq1211234`（可修改 `public/admin/index.html` 中 `ADMIN` 对象的 `passwordHash`，需填 SHA-256 摘要）

#### 后台功能

- **新建文章**：后台"文章 → 新建"填写标题、作者、日期、访问标识、标签、板块、正文（Markdown）
- **编辑/删除**：点击文章列表中的条目即可编辑，可删除
- **上传配图**：正文编辑器可直接上传图片，自动存入 `public/images/articles/`，对应网址 `/images/articles/`
- **发布提交**：点击"发布"即把改动提交（commit + push）到 GitHub 的 main 分支，触发 Actions 自动重新构建部署，网站随即更新

#### 认证方式（DecapBridge）

后台通过 **DecapBridge** 的 git-gateway 服务取得仓库读写权限（与"星川廬"博客后台相同，无需自建 OAuth 服务）：

- `backend.name: git-gateway`，`identity_url` / `gateway_url` 指向 DecapBridge
- 在 https://decapbridge.com 注册站点（绑定本仓库，填入一个有写权限的 GitHub 令牌），把生成的站点 ID 填到 `public/admin/config.yml` 的 `identity_url` 中
- 网站后台当前使用的站点 ID 已配置好；对应 GitHub 令牌存放在 `TOKEN管理/token.txt`

#### 注意事项

- 登录页的账号密码校验在前端完成，仅作为"门禁"，无法做到绝对安全；真正的内容写入权限由 DecapBridge 网关控制
- 文章字段与 `src/articles/` 下的 Markdown Frontmatter 一一对应，后台新建/修改的文章与其他文章同样会被自动扫描加载
- 修改后台账号密码后需重新构建部署才生效

## 样式系统

### Tailwind CSS 自定义颜色

```css
--color-xlys-red: #A00000;      /* 主红色 */
--color-xlys-dark: #333333;      /* 深色文字 */
--color-xlys-gray: #666666;      /* 灰色文字 */
--color-xlys-beige: #F5F5DC;     /* 米色背景 */
--color-xlys-beige-dark: #E8E4D9; /* 深米色边框 */
```

### 占位符样式

缺失图片使用 `.img-placeholder` 类显示虚线边框占位符。

## 常用修改

### 修改导航菜单

编辑 `src/components/Navigation.tsx` 中的 `navItems` 数组：

```tsx
const navItems = [
  {
    name: '首页',
    href: '/',
    children: []
  },
  {
    name: '社团情况',
    href: '/society',
    children: [
      { name: '社团概况', href: '/society' },
      { name: '师林撷英', href: '/society/history' },
      // ...
    ]
  },
  // ...
];
```

**下拉菜单样式说明**：白色下拉菜单定位在菜单名正下方（`top-full left-1/2 -translate-x-1/2 mt-[14px]`），与红色导航栏有 14px 间距。下拉动画过渡时间为 600ms（`style={{ transitionDuration: '600ms' }}`），使用 `opacity` + `max-h` 实现展开/收起效果，通过 `onMouseEnter`/`onMouseLeave` 控制。

### 修改轮播图

编辑 `src/components/Banner.tsx`：
- 轮播图来源：取最近 6 篇金石资讯文章的首图（`.slice(0, 6)`）
- 胶囊进度条：激活态 `h-2 w-8 bg-white`，非激活态 `h-2 w-2 bg-white/40`，间距 `space-x-3`，过渡 300ms
- 底部标题字号：`text-sm`

### 修改社团简介

编辑 `src/pages/admin/SocietyPage.tsx` 中的简介内容。

### 修改社长信息

编辑 `src/pages/admin/PresidentDetailPage.tsx` 中的 `presidentData` 对象：

```tsx
const presidentData: Record<string, {
  name: string;
  term: string;
  photo: string;
  introduction: string;
  achievements: string[];
}> = {
  '钱牧风': { ... },
  '刘馨阳': { ... },
  '张奕茹': { ... }
};
```

### 修改社员名单

编辑 `src/pages/admin/SocietyMembersPage.tsx` 中的 `membersByGeneration` 对象：

```tsx
const membersByGeneration: Record<string, string[]> = {
  '第1届': ['高一1班 钱牧风', ...],
  '第2届': [...],
  // ...
};
```

### 修改活动横幅

编辑 `src/components/ActivityBanners.tsx` 中的横幅内容。每个横幅的 `searchQuery` 字段控制点击后搜索的关键词。

### 修改规章制度

编辑 `src/pages/admin/RulesPage.tsx` 的列表项，`RulesCharterPage.tsx` 的章程内容和 `RulesManualPage.tsx` 的社务手册内容（PDF 通过 iframe 嵌入）。

### 修改章程内容

编辑 `src/pages/admin/RulesCharterPage.tsx`，章程包含五章十四条（总则、组织机构、社务活动、社员权利与义务、附则），每章标题为红色居中显示（`text-xlys-red font-bold text-center`）。

### 修改指导老师信息

编辑 `src/pages/admin/SocietyHistoryPage.tsx`（师林撷英页面）中的老师头像和介绍。照片放入 `public/images/static/` 后以绝对路径 `/images/static/xxx.jpg` 引用。

### 修改快捷链接

编辑 `src/components/QuickLinks.tsx` 中的链接项：

```tsx
const links = [
  { name: '合作申请', href: '/cooperation' },
  { name: '社员名录', href: '/society/members' },
  { name: '社务征稿', href: '/academic/submission' },
  { name: '社刊投稿', href: '/journal/submission' }
];
```

### 修改二级页面翻页设置

所有二级目录页面共用 `src/components/Pagination.tsx` 翻页组件。修改每页显示数量时编辑对应页面的 `PAGE_SIZE` 常量即可。

### 修改组织架构名单

- **社务委员会**（`OrganizationPage.tsx`）：显示时任社长和副社长
- **社团智库**（`OrganizationStandingPage.tsx`）：两列表格显示历任社长（第一列）和对应副社长（第二列）

### 文章链接自动换行

`ArticlePage.tsx` 中自定义 `<a>` 组件，设置 `wordBreak: 'break-all'` 确保长链接自动换行，不溢出容器。链接颜色为社团红 `#c41d1d`，新标签页打开。

### 更新社务手册 PDF

替换 `public/篆刻社社务手册.pdf` 文件即可，`RulesManualPage.tsx` 通过 iframe 自动加载新文件。

## 注意事项

1. 所有文章必须包含 YAML Frontmatter，否则无法正确加载
2. 图片路径使用绝对路径（如 `/images/articles/xxx.png`）
3. 文章文件名格式建议：`YYYY-MM-DD-文章标题.md`
4. 修改代码后需要重新构建才能在生产版本中生效
5. 删除文章后，网站会自动更新，无需手动修改代码
6. 添加新页面需要在 `src/App.tsx` 中配置路由
7. **LOGO 和题字图片必须使用绝对路径**（`/logo.png`、`/题字.svg`），否则子页面可能加载失败
8. 更新网站功能后需要同步更新本指南文件
9. 社务手册 PDF 放在 `public/` 目录下，通过 iframe 嵌入展示

## 技术栈

- React 18
- TypeScript
- Vite 6
- Tailwind CSS 3
- React Router 6
- React Markdown（Markdown 渲染）
- rehype-raw（HTML 支持）
- remark-gfm（GFM 表格等支持）
